
  # CSV Review Interface

  This is a code bundle for CSV Review Interface. The original project is available at https://www.figma.com/design/vRy5H0j9MDt7ZzrmPdQNl2/CSV-Review-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  